environments {
    default{
        webdriver.base.url = "http://localhost:5173/"
    }
}

webdriver {
    driver = chrome
    autodownload = false
    capabilities {
            "goog:chromeOptions" {
                args = [
                    "--remote-allow-origins=*", "--xheadless"
                ]
            }
        }
}